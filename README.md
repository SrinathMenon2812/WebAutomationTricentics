# Playwright Automation

## Trace Viewer

Run tests with trace artifacts enabled:

```bash
npm run test:trace
```

Open the generated trace with:

```bash
npx playwright show-trace <trace-file>.zip
```
